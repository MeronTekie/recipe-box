@{
  Layout ="_Layout";

}

<h1>Here are the list of the Categories </h1>


@using RecipeBox.Models

@if(Model.Count ==0)
{
  <h3>There are no aparent categories to be listed</h3>
}

<ul>
  @foreach(Category category in Model)
  {
    <li> @Html.ActionLink($"{category.Name}","Details", new {id = category.CategoryId})</li>
  }
</ul>

<h4>@Html.ActionLink("Create a new Category","Create")</h4>
<h4>@Html.ActionLink("Home","Index","Home")</h4>